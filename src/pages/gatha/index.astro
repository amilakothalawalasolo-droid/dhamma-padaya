---
// src/pages/gatha/index.astro

// අපි දැන් ෆෝල්ඩර් එකක් ඇතුලේ ඉන්න නිසා පියවර 2ක් පස්සට යන්න ඕනේ (../../)
import MainLayout from '../../layouts/MainLayout.astro';
import chapters from '../../data/chapters.json';
---

<MainLayout title="ධම්ම පදය - පටුන">
    
    <div class="mb-6 mt-2 px-2">
        <h1 class="text-2xl font-bold text-gray-800">ධම්ම පදය</h1>
        <p class="text-gray-500 text-sm mt-1">සියලු ගාථා ඇතුළත් පරිච්ඡේද (වග්ග) 26</p>
    </div>

    <div class="space-y-3 pb-8">
        {chapters.map((chapter) => (
            <a href={`/gatha/${chapter.id}`} class="block bg-white rounded-xl p-4 border border-stone-100 shadow-sm active:scale-98 transition-transform hover:border-amber-200 group">
                <div class="flex items-center justify-between">
                    <div class="flex items-center gap-4">
                        <div class="w-10 h-10 rounded-full bg-orange-50 text-amber-700 flex items-center justify-center font-bold text-sm group-hover:bg-amber-100 transition">
                            {chapter.number}
                        </div>
                        
                        <div>
                            <h3 class="font-bold text-gray-800 text-lg group-hover:text-amber-700 transition">{chapter.pali}</h3>
                            <p class="text-xs text-gray-500">{chapter.sinhala}</p>
                        </div>
                    </div>

                    <div class="text-gray-300 group-hover:text-amber-500">
                        ❯
                    </div>
                </div>
                
                <div class="mt-2 ml-14">
                    <span class="text-[10px] bg-gray-100 text-gray-500 px-2 py-0.5 rounded-full">
                        ගාථා {chapter.count} යි
                    </span>
                </div>
            </a>
        ))}
    </div>

</MainLayout>
